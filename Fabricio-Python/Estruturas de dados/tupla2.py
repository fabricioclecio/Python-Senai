# Cria duas tuplas numéricas.
tupla1 = (1, 2, 3, 4, 5)
tupla2 = (6, 7, 8, 9, 10)

# Cria uma nova tupla, agrupando as duas anteriores.
agrupada = (tupla1, tupla2)

# Exibe a tupla agrupada (multidimensional).
print(agrupada)

# Exibe valores da tupla agrupada (numero 8).
print(agrupada[1][2])

