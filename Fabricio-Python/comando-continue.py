# Laço de repetição de 0 a 5.
for numero in range(5):

    # Verifica se o número é igual a 2.
    if numero == 2:

        # Continue, para a execução atual.
        continue
    else:
        print(numero)